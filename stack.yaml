version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8080
functions:
  gmail-filter:
    lang: python3-http
    handler: ./gmail-spam-detection
    image: ttl.sh/welteki/gmail-spam-detection:0.0.4
    secrets:
      - openai-api-key
      - token.json
    annotations:
      topic: gmail-notifications
